# Create a question class which needs two attributes, 'text' and 'answer' to initialize
class Question:

    def __init__(self, text, answer):
        self.text = text
        self.answer = answer
